group = 'org.apache.isis.viewer'
description = 'Apache Isis Viewer - RO (Viewer)'
dependencies {
    compile project(':isis-core-webapp')
    compile project(':isis-viewer-common')
    compile project(':isis-viewer-restfulobjects-rendering')
    compile(Libs.jacksonModuleJaxbAnnotations)
    testCompile project(':isis-core-internaltestsupport')
    compileOnly(Libs.javaxJdo)
}

task packageTests(type: Jar) {
    from sourceSets.test.output
    classifier = 'tests'
}
artifacts.archives packageTests
