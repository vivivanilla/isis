group = 'org.apache.isis.subdomains'
description = 'Apache Isis Sub - Excel (Fixtures)'
dependencies {
    compile project(':isis-subdomains-excel-applib')
    compile project(':isis-subdomains-excel-testing')
    compile project(':isis-applib')
    compile project(':isis-testing-fixtures-applib')
    compile project(':isis-persistence-jdo-datanucleus5')
}

task packageTests(type: Jar) {
    from sourceSets.test.output
    classifier = 'tests'
}
artifacts.archives packageTests
