group = 'org.apache.isis.valuetypes'
description = 'Apache Isis Val - Markdown (persistence jdo-dn5)'
/*apply plugin: 'war'
sourceSets {
    main {
        java {
            srcDirs += "${buildDir}/target/classes"
        }
    }
}*/
dependencies {
    compile(Libs.datanucleusCore)
    compile project(':isis-valuetypes:isis-valuetypes-markdown:isis-valuetypes-markdown-applib')
}

task packageTests(type: Jar) {
    from sourceSets.test.output
    classifier = 'tests'
}
artifacts.archives packageTests
