group = 'org.apache.isis.legacy'
description = 'Apache Isis Legacy - Metamodel'
apply plugin: 'war'
dependencies {
    compile(Libs.guava)
    compile project(':isis-applib')
    compile project(':isis-legacy-applib')
    compile project(':isis-core-metamodel')
    testCompile project(':isis-core-internaltestsupport')
    compileOnly(Libs.javaxJdo)
    providedCompile project(':isis-persistence-jdo-applib')
}

task packageTests(type: Jar) {
    from sourceSets.test.output
    classifier = 'tests'
}
artifacts.archives packageTests
